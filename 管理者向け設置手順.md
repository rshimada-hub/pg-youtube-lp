# 工務店YouTube制作サービスLP ― 設置手順（管理者向け）

このフォルダ一式（`index.html` / `css/` / `js/` / `img/` / `works.html`）を、
自社HP（pure-growth.co.jp）の任意のサブディレクトリにアップロードすれば公開できます。
すべて**相対パス**なので、置いたディレクトリがそのまま公開URLになります。

例：`/lp/koumuten-youtube/` に置く場合
```
/lp/koumuten-youtube/index.html
/lp/koumuten-youtube/css/style.css
/lp/koumuten-youtube/js/main.js
/lp/koumuten-youtube/img/...
```

## フォームについて（重要）
このLPの申込フォームは、**WordPress側の MW WP Form** で処理します（静的ファイル単体ではメール送信できないため）。
LPの申込セクション（`#contact`）には、その **WordPressフォームを iframe でインライン表示**しています。
別ページに飛ばさないので、来訪者はLP上で申込を完結できます（離脱しません）。

- 埋め込み先フォームページ：**https://pure-growth.co.jp/koumuten-youtube-form/**
- MW WP Form ID：**`[mwform_formkey key="11835"]`**（フォーム名：資料DL（工務店YouTube制作LP））

### このフォームの動作（設定済み・追加設定不要）
1. 申込者に **自動返信メール** を送信（件名「資料ダウンロードリンクをお送りします【ピュアグロース】」、本文に資料のGoogle Driveリンク＝「集客と育客を両立する 最新YouTubeノウハウ」資料）
2. **info@pure-growth.co.jp に通知メール**（リード内容一式）
3. リードを **問い合わせデータ（DB）** に自動保存
4. **reCAPTCHA v3**（迷惑送信よけ）
5. 送信後は **同じ画面内にサンクスメッセージ**を表示（ページ遷移なし）

※配布資料は既存の資料DLページ（/download/youtube/ ＝ ダウンロード投稿 11766）と同一の資料です。

## 設置パターン

### パターンA：このまま静的ファイルで設置（推奨・そのまま動きます）
- フォルダ一式を自社HP（pure-growth.co.jp）配下にアップロードするだけ。
- フォームは iframe（`https://pure-growth.co.jp/koumuten-youtube-form/`）で表示されます。
- **同一ドメイン配下**なので、iframeは中身の高さに自動フィットします（`js/main.js` が調整）。

### パターンB：WordPressの固定ページとして作る場合（任意・より自然）
- 固定ページを作り、LP本文を流し込み。
- `index.html` 内の申込セクションにある `<iframe id="pgFormFrame" …>` の代わりに、
  ショートコード **`[mwform_formkey key="11835"]`** を直接置いてください（テーマのフォーム装飾がそのまま効きます）。

## 変更したい場合
- 文言・構造 → `index.html`
- デザイン・色 → `css/style.css`
- 動き（カウントアップ・スクロール表示・動画・iframe高さ調整）→ `js/main.js`
- 申込フォームの項目・自動返信メール・配布資料リンク → WordPress管理画面 ＞ MW WP Form ＞「資料DL（工務店YouTube制作LP）」（ID 11835）

## 制作
ピュアグロース株式会社
